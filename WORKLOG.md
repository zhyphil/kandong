# 工作记录

## 2026-09-23 · Phase0 开始

- 检查空目录、Android Studio/SDK/JDK/Gradle/Kotlin环境；初始化独立main，SSH origin只读连接成功且远端为空。没有推送。
- AO只读单步骤技术规划完成；受限实现步骤在600秒时限结束，已落地代码保留，由主执行者接续修复、补文档与验证。未把超时状态算成功。
- 新建Kotlin/Compose工程、节点读取/会话/覆盖层、合成测试App、单元/设备测试及产品、安全、架构、MVP和来源文档。
- 首次编译定位未公开View窗口属性，改用公开createAccessibilityNodeInfo().windowId，并跟踪自身窗口移除事件。
- 测试页敏感标记改为公开setter；设备权限断言允许AndroidX自身signature权限；缺失服务连接必须失败而非跳过设备测试。
- 构建与设备验证进行中。最终结果、限制及下一步在验收后更新。

## 2026-09-23 · 产品方向校正与Phase0A

- 用户明确纠正为“跨App局部屏幕放大镜 + 放大区域翻译”，顺序改为看清→看懂→理解→会操作；停止扩展旧AI/目标引导路线。
- 完成全项目文件盘点。将旧代码/文档保留为本地提交68f3bf9；旧服务/UI/配置/设备测试保存至experiments/guidance，底层过滤/几何单测保留；明确旧完整设备套件3项1失败。
- AO只读规划一项149秒完成；有界原生实现一项316秒完成；工作者因Gradle缓存写权限未完成标准构建，由主执行者使用正常授权环境完成，不把工具限制当代码缺陷。
- 重写产品/架构/MVP/安全/README并新增官方MAGNIFICATION比较。优先官方系统窗口+Controller，不请求节点/截图/手势。核验API33/34分支、源区并非镜框目标矩形、无公共尺寸setter。
- app实现原生2/3/4倍、跨App控制条、同意撤回、停止/重连边界与已有放大保护。fixture新增小字、网格、点击计数及自身布局指纹。
- 用户连接HMA-L29/API29后要求旧华为支持；又换成目标LIO-AN00/API31。两机实际feature=false，原生路线No-Go。新增独立compat屏幕共享区域裁剪实验，明确其整屏内存缓冲区与分离源框/镜面限制。
- 主执行者实际构建app/compat/fixture，29+4单测通过，Lint无错误，app/compat发布构建通过。API36模拟器3项集成测试通过并保存实测2倍/4倍/Settings截图。没有上传或发布。
- 真机安装到达华为风险确认页，等待用户安装和投屏权限；尚未把真机视觉/触摸/停止算通过。下一步按VALIDATION完成目标机矩阵，再判断0A Go/No-Go，随后才开始0B区域翻译。

- 独立只读审查128秒完成，发现兼容取景边框alpha=1的Android12触摸阻断、原生启动旧计时器影响新会话。已分别按平台最大遮挡透明度和整个callback generation守卫修复。修复后调试/发布构建、33单测及Lint再次通过。
- 兼容版在API36模拟器真实启动屏幕共享，拖动源框并验证框内按钮0→1且布局指纹不变；2/3/4倍率源框尺寸正确；点停止后投屏为null且前台服务不存在。安全镜面截图是黑块，未把它当图像质量证明。
- 取消尚未确认的审查前旧APK安装会话，并重新提交修复后的最新APK，避免手机装到触摸有缺陷的旧版。新安装仍需用户在手机确认；没有改变手机系统安全设置或代开权限。

- 用户反馈纯净模式增强防护拦截，已核对华为官方说明并告知手机端手动路径及测试后恢复；没有自动关闭系统防护。

- 最新手机安装返回INSTALL_FAILED_ABORTED（User rejected permissions），未安装成功；需要用户按官方说明处理增强防护后重新安装。当前无仍在运行的手机安装任务；仓库本地提交完成，未推送。

## 2026-09-23 · 华为安装与首个可见画面

- 用户手动关闭增强防护后，最新compat与fixture均安装成功；悬浮窗权限、屏幕共享及前台服务已实读确认。
- 用户在真实手机确认放大画面与红色源框对应。记录初始窗口几何与合成页布局指纹；完整倍率、触摸和停止矩阵仍待验证。
- 因检测到手机离开合成页，自动化在任何输入前中止，等待协调手机使用；未向第三方App注入测试点击。
- 同步修正MVP“本阶段不截图”的过宽表述，准确区分官方原生路线与用户授权的华为兼容屏幕帧路线。

- 用户将手机交给测试后，实体LIO-AN00上2/3/4倍率源区、拖动、框内一次触摸、布局指纹不变均通过；点击停止后MediaProjection/服务/窗口全部释放。结构化结果保存至docs/evidence/phase0a-huawei-runtime.json。
- 已返回首页，用户继续进行浏览器与第三方App、锁屏的手动体验检查；这部分尚未用合成页结果替代。文档变化执行diff检查，未因纯记录更新重复构建或全量测试。

- 用户补充：在“bookings”中原页面保持原样，锁屏再解锁后放大窗消失。随后独立检查投屏null、服务nothing、悬浮窗不存在，确认锁屏结束会话且解锁不自动恢复。未读取第三方App内容，未将用户名称当成已核对的包名/版本。
- README补充锁屏消失属于预期行为及重新打开方法；浏览器体验仍等待用户反馈。

## 2026-09-23 · 顶部取景修复

- 用户确认浏览器倍率、拖动和滚动跟随正常，排版不变，同时报告手柄占据上方导致顶部不可达。
- NORMAL有界修复：按系统化调试定位80dp顶部限制与固定上方手柄；采用现有JUnit回归，无新框架、无代理、无外部服务。
- 新增SourcePlacement，源区可达屏幕边缘，手柄随显示窗方向放在下方/上方，拖动时不跳边；边框按屏幕裁剪，内部触摸穿透不变。
- 对旧几何算法新增4项回归均失败，修复后compat共8项通过；兼容模块debug/release构建、Lint通过。正在向目标华为安装，等待用户重新屏幕共享并核对顶部画面。

- 修复版安装Success，用户确认可拖到顶部并看清内容；窗口几何确认手柄位于源区下方。此次没有读取第三方页面内容，也未代替用户确认屏幕共享。顶部问题验收完成，更多App/长辈体验及后续翻译仍保留任务状态。

## 2026-09-23 · 自由取景与自动避让

- 用户要求取消固定2/3/4倍及手动换边，改为拖动取景框大小自动计算倍率、根据取景位置自动放置显示窗。该要求覆盖此前固定倍率交互，官方app实验暂保持原状。
- COMPLEX有界阶段，最多三项：AO只读方案核查、兼容代码/现有测试实现、只读审查。只读核查323.4秒完成；提出自由矩形、等比显示、24dp中线缓冲、冻结活动手柄朝向和松手贴边，避免自动避让打断手势。
- 当前实现范围仅compat几何/手势/服务/首页和JUnit；主执行者同步产品、路线与使用说明。没有新增权限、网络或页面操作能力。新交互验证完成前不沿用旧倍率真机通过结论。

- AO实现467.8秒完成，独立审查115.4秒发现1项静止页面持续空白问题。主执行者在模拟器快速点按复现后修正首帧节流；同场景连续6次恢复正常，未重复付费审查。
- 主执行者去掉图片生产者时间戳与系统时钟的比较，采用清空旧图/丢弃队列帧；设无密度Bitmap匹配像素倍率，并增加仅debug的几何/帧计数标准dump，未输出页面内容。
- Gradle兼容模块debug/release构建、21单测、Lint通过；专用模拟器验证非预设倍率、独立宽高、双向自动避让、上下贴边、布局/计数不变、框内点击和停止释放。已记录测试脚本字段差异，未把脚本解析失败当产品问题。
- 正在更新目标华为并请用户实看新版自由缩放/自动避让；旧固定倍率验收不替代本次结果。

- 新版安装成功后，用户报告角柄可调大小，但倍率数字与视觉不一致。只读目标机几何467×351→1096×481，计算1.37037；尚无实际矩阵/视觉对照，不把数学公式当成解决证据。
- NORMAL有界调试：仅增加debug的drawable/矩阵诊断，无页面内容输出。模拟器复现独立缩窄而倍率不变，按显示窗比例改为角柄连续等比缩放，保留对角锚点/边界/自动避让。两项行为回归先红后绿，含边界回归23单测与debug/release/Lint通过。
- 专用模拟器实际2.00→2.76→2.89→2.33倍，计算值与真实绘制矩阵逐项一致；自动上下、两端、触摸穿透/原页布局及停止释放复验通过。目标机视觉不一致仍等待同画面数值与用户反馈；未扩大为全部华为兼容结论。

- 比例缩放版已安装真机并恢复用户共享；停止手势后的抽样计算/实际矩阵同为1.2655889（显示1.27×）。记录数值一致但不替代视觉验收。最后仅更新首页操作说明并重新构建APK，不中断用户当前测试；等待其具体差异描述。

## 2026-09-23 · 用户照片核对

- 只读核对照片手机时间19:38与目标包更新时间19:43:23，照片对应等比例版安装前。照片1.06倍/大片侧留白支持旧版独立宽高的限制方向问题；不将其错归为新版回归。
- 新版真机源框/Bitmap/drawable282×124、镜面1096×481，计算与真实矩阵均3.8790324，横向总留白约2.1px。已向用户说明并等待新版视觉确认。本次只更新验证记录，无代码改动，不重复构建。

- 用户随后明确确认“正常了，文字变大且没有大片留白”。已关闭本次缩放/留白问题，保留新交互完整真机场景与长辈体验待测，不将单项确认扩大为整个Phase0A完成。

## 2026-09-23 · 自由选区与独立倍率滑杆

- 用户明确将规则改为自由调整取景宽高、另设1～5倍滑杆（默认2倍），并同意超出镜面时滑动查看。同步替换工程/产品约束，不把旧等比例规则当成额外审批门槛。
- NORMAL有界实现，复用现有JUnit/验证方法，无代理/AO调用。SourceGesture恢复独立宽高；MagnifierViewport维护精确倍率与两轴滚动；ImageView MATRIX保证实际绘制倍率，滑杆与取景互不修改。新会话重置2倍；选区变化回到镜面起点。
- 两项独立宽高回归先失败再通过；28单测、调试/发布构建、Lint成功。模拟器实触验证1/2/2.63/5倍、框宽高独立变化、镜面两轴首末边缘、自动换位/屏幕边缘、原布局与点击计数、停止/新会话默认值。
- 更新到0.0.2-freezoom（versionCode2），已提交目标华为安装请求，等待用户安装/共享及新规则反馈。没有改变原生app/fixture或权限。

- 0.0.2-freezoom目标华为安装成功；用户确认“符合预期，三个操作都正常”。只读抽样倍率/矩阵均2.44、滑杆144、镜面偏移0/30、显示窗位于上方。已记录人工与运行证据，不复用旧等比例交互结论。专用模拟器测试会话已停止；保留用户当前手机会话。

## 2026-09-23 · 可收起的日常UI / UX

- 用户确认核心后要求专业简洁界面：收起成贴边放大镜、随用随开、图标控制与未来翻译/账户/付费菜单。保留自由选区、独立倍率和镜面滑动，不扩展到实际翻译/登录/支付。
- COMPLEX预算最多3个专职步骤：只读设计、单实现、独立只读审查；AO只读设计202.6秒完成。核对官方VirtualDisplay.setSurface(null)及48dp触摸建议，收起/菜单暂停画面处理但保留同一次共享，恢复不再createVirtualDisplay。
- 实现阶段由工作者独占compat代码/测试，主执行者维护产品/UX/验收文档并负责后续实际构建和设备检查。新增docs/UX.md作为界面与状态规则，当前仍为实现/验证中。

- AO实现347.4秒、独立只读审查164.0秒；两项P2已修复：迟到菜单回调不能越过终止状态，API29使用真实系统栏/刘海inset和方向校验。主执行者另简化首页披露、合并账号与订阅，隐藏已授权会话的重复同意卡。没有新增依赖、权限或网络。
- compat最终39单测、debug/release/Lint通过；专用API36验证暂停不复制、原App可操作、10次恢复保留选区/5倍/镜面位置且仅1个VirtualDisplay、菜单帮助/隐私/返回、原有选区/倍率/避让/首末行/穿透。工具栏/菜单/首页结束、收起锁屏、菜单180度旋转均清理，解锁不自动恢复。
- 记录测试脚本的窗口/系统授权返回时序及合成页滚动位置问题，修正测试前置条件后通过，未把元数据当成保护窗口的视觉证据。独立审查后没有重跑付费流程。API29实际设备、主动撤销/通知按钮本轮实点、TalkBack/大字号/长期体验仍未验收。
- 0.0.3-compact已安装目标华为并打开看懂首页，等待用户重新共享后的UI/收起/恢复确认；未自动点击私人页面。新增UX规则、实际记录与自身首页图。继续仅本地开发，不推送/发布。

- 用户对新版真机反馈“有问题，我来描述”，等待具体表现；不标为验收通过、不重启其共享或操作页面。只读服务抽样确认运行中、矩阵2.08与滑杆一致，不能替代UX反馈。

- 用户进一步明确问题仅指当前图标表达：收起/关闭含义不直观，悬浮放大镜中的加号不合适。下一步NORMAL局部视觉调整，使用横线/叉号与短标签、纯放大镜，不扩展会话逻辑或新增AO步骤。

- 图标局部调整：横线收起、叉号关闭，各配短小标签且保留48dp点击范围；悬浮与首页放大镜去掉加号，统一简洁线条。保持所有手势与会话逻辑不变；本次为视觉/文案调整，不新增测试框架或AO调用。

- 图标版debug/release/Lint通过，专用模拟器实际点按新工具控件，收起/恢复/关闭均通过；共享与窗口释放正确。保留既有39项逻辑测试，不为绘图追加镜像测试。已向目标华为提交本次更新，等待安装和图标反馈。

## 2026-09-23 · 二级菜单与关闭文案

- 用户指出帮助/隐私等二级页面底部重复“关闭菜单/结束共享”，并认为后者像退出程序。采用更准确的“关闭放大镜”，说明同时停止屏幕共享，保持原来的结束会话行为，不新增退出进程逻辑。
- NORMAL局部范围，直接使用完成前验证方法，零AO/代理。二级页改为固定顶部返回箭头/标题/关闭图标，正文独立滚动，无底部退出操作；主菜单单独保留关闭放大镜。首页、通知、读屏标签和帮助说明统一用语，导航点击区域仍48dp。
- 上次图标更新的安装任务本轮已返回Success；本次菜单版正在进行专用模拟器导航与停止验证，尚不把构建成功当作真机体验通过。

- 专用API36复现并修复首页二级对话框系统返回直接关闭：API33+注册/注销官方返回回调，并保护迟到事件；旧版返回键处理不依赖新API。
- 最终debug/release/Lint通过，6项菜单设备检查通过；二级页不再出现底部退出按钮，顶部导航滚动时固定，关闭可恢复原展开/收起状态，主菜单关闭放大镜释放全部资源。复用39项状态/几何单测证据，没有声称本轮重跑全套。文档与自身首页截图已同步，正在向目标华为安装本次修订。

- 本次菜单版目标华为安装返回Success，已打开看懂自身首页；未自动确认屏幕共享或操作其他App。代码/设备回归完成，真机主观体验仍等待后续反馈。
