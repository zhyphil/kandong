# 看懂工程约束

- 本仓库是独立 Android 项目。不得读取、修改或复用 SpotAva/Parknow 文件、构建目录、模拟器或配置。
- 产品只做“解释 → 高亮 → 用户亲自点击”。生产代码禁止节点动作、手势注入、自动点击、输入及全局动作。测试代码可操作独立测试应用。
- 页面节点只因用户点击读取而采集；跳过密码、输入和敏感子树，原始内容不写日志、不落盘、不发送网络。
- 停止、过期、页面变化和服务退出必须清除内存与高亮。未知窗口状态应拒绝，不猜测目标。
- 验证结论必须指向实际命令和输出；缺依赖是环境阻塞。单元测试不能替代跨应用、触摸穿透及实体设备验收。
- 改动后同步 WORKLOG.md/TASKS.md；保留已有变更。未经明确授权不提交、不推送、不部署。
