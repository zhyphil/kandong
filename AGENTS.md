# 看懂工程约束

- 唯一项目根目录是本目录。不得读取、修改、引用或复用其他无关项目的文件、模拟器或配置。
- 产品顺序固定为 **看清 → 看懂 → 理解 → 会操作**。当前第一任务是 Phase 0A 跨 App 局部放大镜；0B 区域翻译；1 简单中文解释；2 节点高亮引导；3 长辈体验/TTS/性能增强。隐私和停止能力从第一阶段实施。
- 目标华为兼容版交互以自由调整取景框宽高 + 独立1～5倍连续滑杆（每次新会话默认2倍）+ 自动上下避让为准。选区大小与倍率互不影响；放大后超出镜面的内容可在镜面内滑动查看。不得恢复等比例绑定、按框大小自动决定倍率、固定2/3/4菜单或手动换边。
- 红框内部及边框直接拖动，不再提供单独移动图标；右上角为24dp小角标/48dp触摸区，独立调整宽高。镜面单指平移、双指缩放1～5倍并同步数字/滑杆；操作底层App先收起，不恢复红框内部触摸穿透。
- 主菜单与帮助/隐私二级页均全屏显示，内容避开系统栏/刘海；关闭菜单恢复进入前的展开/收起状态，不恢复为居中的小浮窗。
- 日常体验支持收起为可拖动贴边悬浮按钮并恢复原选区/倍率/镜面偏移；收起与菜单时暂停画面处理，结束/锁屏/撤销/旋转清理全部窗口。图标保持至少48dp可点范围。翻译/账户/订阅菜单仅为明确不可用的后续入口，不接入假登录或购买流程。
- 不更改底层 App layout、系统字体或显示密度；不做 AI 自动点击、输入、确认、提交、购买、支付。生产代码禁止节点动作、手势注入和全局动作。
- Phase 0A 的 `app` 使用官方 Window Magnification + MagnificationController，无节点内容采集、无截图、无联网。不得在失败时静默切换全屏放大或修改用户系统放大设置。用户已明确要求支持华为 LIO-AN00/API31，独立 `compat` 允许经每次屏幕共享授权的本机帧裁剪；必须准确披露整屏缓冲区，不保存/上传，不将此权限扩展为OCR/AI授权。
- 旧引导原型保存在 experiments/guidance，不加入产品构建。其代码及未通过的测试记录不是当前能力或验收证据。
- 后续区域文本采集优先 Accessibility Tree；按用户需要才做截图/OCR fallback。敏感内容先过滤；页面内容不写日志、不默认落盘或上传。
- 尊重用户已有放大状态。系统服务开关由用户手动开启；进程重连不自动恢复会话。停止、锁屏、断开清理本次辅助状态。
- 实际 build/test 后才可报告通过；模拟器不代表真机，不声称所有 App/OEM 兼容。每次同步 WORKLOG.md/TASKS.md。
- 原请求已授权本地开发和清晰的本地提交；未经明确授权不推送、不部署、不发布。保留已有工作。
